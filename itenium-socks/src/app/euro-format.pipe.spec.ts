import { EuroFormatPipe } from './euro-format.pipe';

describe('EuroFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EuroFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
